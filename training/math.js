/** @format */

console.log(Math.PI);

// it gives round number
var num = 10.252;
console.log(Math.round(num));

// pow() => Math.pow(x,y) returns the value of x to the power of y
console.log(Math.pow(3, 3));

console.log(Math.sqrt(83));

// Math.abs() return the absolute value (positive) of x
console.log(Math.abs(-55));

// Math.ceil() return the value of x rounded up to its nearest integer
console.log(Math.ceil(4.2));

// Math.min() => can be used to find the lowest value in a list of argument
// Math.max() => can be used to find the highest value in a list of argument

// console.log(Math.min(10, 9, 8));
console.log(Math.max(10, 9, 8));

// random() => returns a random number between 0(inclusive), and 1
// console.log(Math.random() * 100);
// console.log(Math.floor(Math.random() * 100));

// Math.trunc() => the trunc method returns the integer part of a number
console.log(Math.trunc(-4.62));
console.log(Math.trunc(99.62));
